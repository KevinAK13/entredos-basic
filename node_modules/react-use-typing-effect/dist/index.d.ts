export interface Props {
    over: string | string[];
    charSpeed?: number;
    loop?: boolean;
    delayEnd?: number;
}
declare function useTypingEffect({ over, charSpeed, loop, delayEnd, }: Props): string;
export default useTypingEffect;
